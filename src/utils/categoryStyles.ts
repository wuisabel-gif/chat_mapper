/**
 * Category colors now live with the rest of the category config in
 * src/data/categories.ts (single source of truth). Re-exported here so existing
 * imports keep working.
 */
export { categoryColor } from "../data/categories";
